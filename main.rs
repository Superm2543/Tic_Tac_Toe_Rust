use eframe::egui::{CentralPanel, Context, FontFamily, FontId, TextStyle, TopBottomPanel};
use eframe::{egui, Frame};
#[derive(Default)]
struct App{

}
impl eframe::App for App {
    fn update(&mut self, ctx: &Context, _frame: &mut Frame) {
        
    }
}
fn main() {
    println!("Hello, world!");
}
